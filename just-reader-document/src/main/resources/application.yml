# 文档上传服务配置
server:
  port: 8083
  servlet:
    context-path: /document

spring:
  application:
    name: just-reader-document
  
  servlet:
    multipart:
      max-file-size: 100MB
      max-request-size: 200MB
      enabled: true
  
  datasource:
    url: jdbc:mysql://localhost:3306/just_reader?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQL8Dialect

# 文档上传配置
document:
  upload:
    max-file-size: 104857600  # 100MB
    chunk-size: 5242880       # 5MB
    temp-dir: /tmp/just-reader/uploads
    storage-dir: /data/just-reader/documents
    max-concurrent-uploads: 5
    chunked-upload-enabled: true
    chunked-upload-expiry-minutes: 60
    validation-enabled: true
    virus-scan-enabled: false
    auto-parse-enabled: true
    parse-timeout-seconds: 300
    supported-types:
      - application/pdf
      - application/epub+zip
      - application/vnd.openxmlformats-officedocument.wordprocessingml.document
      - text/plain
      - application/msword
    supported-extensions:
      - .pdf
      - .epub
      - .docx
      - .doc
      - .txt

# 日志配置
logging:
  level:
    com.yearsalso.document: DEBUG
    org.springframework.web: INFO
  file:
    name: logs/document-service.log
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n"
    file: "%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n"

# Swagger配置
springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html
    operations-sorter: method
    tags-sorter: alpha